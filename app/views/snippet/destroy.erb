<div class="col-md-6 col-md-offset-3">
	<div class="col-md-12">
		<h3>Are you sure you want to delete "<%= @snippet.title %>"? This action is irreversible and will cause you to lose any reputation points gained from it.</h3>
		<a href="/snippets/<%= @snippet.id %>/<%= @snippet.slug %>"> Back to Safety</a>
		<form action="/snippets/<%= @snippet.id %>/delete" method="POST">
			<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
			<input type="submit" value="Delete Snippet" />
		</form>
	</div>
</div>